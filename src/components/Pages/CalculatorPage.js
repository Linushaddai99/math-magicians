import React from 'react';
import Calculator from '../Calculator/Calculator';

const CalculatorPage = () => (
  <div className="container" id="calc-page">
    <h2>Lets Do some Maths</h2>
    <Calculator />
  </div>
);

export default CalculatorPage;
